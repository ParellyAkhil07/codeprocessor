
public class MakeChange {

    // Function to compute the number of ways to make the amount with given denominations
    public static int makeChange(int amount, List<Integer> denominations, int index) {
        // Base case: if amount is zero, there's one way to make the change (using no coins)
        if (amount == 0) {
            return 1;
        }

        // Base case: if amount is negative, there's no way to make the change
        if (amount < 0) {
            return 0;
        }

        // Base case: if no more denominations are available, no way to make the change
        if (index == denominations.size()) {
            return 0;
        }

        int currentCoin = denominations.get(index);
        int combos = 0;

        // Try using the current coin and reduce the amount accordingly
        while (amount >= 0) {
            combos += makeChange(amount, denominations, index + 1);
            amount -= currentCoin;
        }

        return combos;
    }

    public static void main(String[] args) {
        // Test cases
        List<Integer> denominations1 = List.of(1, 2, 3);
        System.out.println(makeChange(4, denominations1, 0)); // Output: 4

        List<Integer> denominations2 = List.of(5, 10);
        System.out.println(makeChange(20, denominations2, 0)); // Output: 3
    }